export type AssistantPageContext = {
  id: "metal-cassette-calculator" | "dxf-online-order" | "general";
  label: string;
  greeting: string;
  knowledge: string;
  suggestions: string[];
};

export const steelProduktBrandKnowledge = [
  "«Сталь Продукт» — бренд, а не наименование юридического лица.",
  "Под брендом представлены инженерные решения и производство изделий из листового металла для строительства, промышленности и инженерной инфраструктуры.",
  "Публично описанная технологическая цепочка включает инженерно-конструкторскую подготовку, лазерный раскрой, гибку, сварку, сборку, подготовку поверхности, порошковую окраску, контроль качества, комплектацию, упаковку и организацию отгрузки.",
  "Монтаж на объекте не выполняется.",
].join(" ");

const generalContext: AssistantPageContext = {
  id: "general",
  label: "Консультация по производству",
  greeting: "Опишите изделие, производственную задачу или спросите о возможностях «Сталь Продукт». Я помогу разобраться и подготовить технически корректную заявку.",
  knowledge: `Посетитель находится на общей странице сайта. ${steelProduktBrandKnowledge} Отвечай по подтверждённым данным о продукции, технологиях, подготовке исходных данных и порядке передачи задачи инженеру. Не придумывай характеристики, цены, сроки, наличие или нормативное соответствие.`,
  suggestions: [
    "Что вы производите?",
    "Возможности производства",
    "Как подготовить чертёж?",
    "Как получить расчёт?",
  ],
};

const contexts: Array<{ match: (pathname: string) => boolean; value: AssistantPageContext }> = [
  {
    match: (pathname) => pathname === "/calculator-metallokassety",
    value: {
      id: "metal-cassette-calculator",
      label: "Калькулятор металлокассет",
      greeting: "Вы открыли калькулятор металлокассет. Могу подробно объяснить, как им пользоваться, что означает формат кассеты, как читать результат и когда нужен чертёж для проверки инженером.",
      knowledge: [
        steelProduktBrandKnowledge,
        "Текущая страница — публичный калькулятор металлокассет.",
        "Пользователь выбирает стандартный формат кассеты и указывает количество в штуках, затем запускает расчёт.",
        "Результат является предварительным и показывает ориентировочную общую стоимость, цену за одну штуку, оценку массы и срок изготовления.",
        "Страница также использует геометрию заготовки для оценочного расчёта.",
        "Для нестандартных размеров и больших партий инженер уточняет цену после проверки чертежа и раскладки.",
        "Не выдавай предварительный результат калькулятора за окончательное коммерческое предложение.",
      ].join(" "),
      suggestions: [
        "Как пользоваться калькулятором?",
        "Что означает формат кассеты?",
        "Как читать результат расчёта?",
        "Когда нужен чертёж инженеру?",
      ],
    },
  },
  {
    match: (pathname) => pathname === "/online-order",
    value: {
      id: "dxf-online-order",
      label: "Онлайн-заказ по DXF",
      greeting: "Вы открыли онлайн-заказ по DXF. Могу объяснить подготовку файла, загрузку, автоматический предварительный расчёт и какие данные указать перед отправкой инженеру.",
      knowledge: [
        steelProduktBrandKnowledge,
        "Текущая страница — публичный онлайн-заказ по DXF.",
        "На этой странице пользователь загружает DXF с контуром детали; система автоматически оценивает длину реза, примерное время обработки и ориентировочную стоимость лазерной резки.",
        "Файл загружается в выделенную закрытую папку на Яндекс Диске, после чего ссылка автоматически подставляется в заявку.",
        "Пользователь дополняет заявку материалом, толщиной, количеством и комментарием.",
        "После отправки специалист проверяет геометрию, материал и производственные параметры.",
        "Публичный автоматический расчёт на этой странице предназначен именно для DXF. Не утверждай, что STEP/STP автоматически рассчитывается здесь. STEP/STP и другие поддерживаемые исходные форматы можно передать через инженерную заявку для проверки специалистом.",
        "Загруженный DXF используется для технологической оценки и подготовки коммерческого предложения.",
      ].join(" "),
      suggestions: [
        "Как подготовить DXF?",
        "Как считается длина реза?",
        "Что указать в заявке?",
        "Можно передать STEP?",
      ],
    },
  },
];

export function normalizeAssistantPathname(value?: string | null) {
  if (!value || typeof value !== "string") return "/";
  const pathname = value.split(/[?#]/, 1)[0].trim();
  if (!pathname.startsWith("/") || pathname.length > 180) return "/";
  return pathname.replace(/\/{2,}/g, "/");
}

export function getAssistantPageContext(value?: string | null): AssistantPageContext {
  const pathname = normalizeAssistantPathname(value);
  return contexts.find(({ match }) => match(pathname))?.value ?? generalContext;
}

export function assistantSuggestionsForPage(
  query: string,
  pathname: string | null | undefined,
  fallback: string[],
) {
  const context = getAssistantPageContext(pathname);
  const text = query.toLowerCase();
  const asksAboutPage = /калькулятор|как пользоваться|поле|формат|результат|dxf|step|загруз|длина реза|заготов|масса/.test(text);
  return context.id !== "general" && asksAboutPage ? context.suggestions : fallback;
}

export function pageSpecificKnowledgeAnswer(query: string, pathname?: string | null) {
  const context = getAssistantPageContext(pathname);
  const text = query.toLowerCase();

  if (/сталь продукт|о вас|про вас|расскаж.*бренд|расскаж.*компан|что вы производите|чем занимаетесь|кто вы/.test(text)) {
    return `${steelProduktBrandKnowledge} Помощник может подробнее рассказать о продукции, технологических операциях, подготовке чертежей и порядке получения расчёта.`;
  }

  if (context.id === "metal-cassette-calculator") {
    if (/как пользоваться|как работает|калькулятор|что делать|инструкц/.test(text)) {
      return "Калькулятор работает в три шага: 1) выберите стандартный формат металлокассеты; 2) укажите количество в штуках; 3) нажмите «Рассчитать». В результате появятся ориентировочная общая стоимость, цена за 1 шт., оценка массы и срок изготовления. Расчёт предварительный: для нестандартных размеров и больших партий инженер проверяет чертёж и раскладку.";
    }
    if (/формат/.test(text)) {
      return "Поле «Формат кассеты» выбирает один из стандартных вариантов, заложенных в калькулятор. Если нужного размера нет, не подменяйте его ближайшим стандартом: передайте инженеру чертёж или размеры для отдельного расчёта.";
    }
    if (/масса|вес/.test(text)) {
      return "Масса в калькуляторе — оценочный показатель, связанный с выбранным форматом, количеством и геометрией заготовки. Для нестандартного изделия итоговую массу и раскладку нужно подтверждать по чертежу.";
    }
    if (/результат|стоим|цен|срок/.test(text)) {
      return "Результат калькулятора — предварительная оценка: общая стоимость, цена за одну кассету, масса и срок изготовления. Это не окончательное коммерческое предложение; нестандартные размеры, крупные партии и особенности проекта проверяет инженер.";
    }
  }

  if (context.id === "dxf-online-order") {
    if (/step|stp/.test(text)) {
      return "Автоматический онлайн-расчёт на этой странице рассчитан на DXF. STEP/STP можно передать через инженерную заявку как исходный файл для проверки специалистом, но не следует считать, что он автоматически рассчитывается DXF-калькулятором.";
    }
    if (/как пользоваться|как работает|загруз|dxf|инструкц|что делать/.test(text)) {
      return "Порядок такой: 1) загрузите DXF с контуром детали; 2) файл попадёт в выделенную закрытую папку на Яндекс Диске, а ссылка автоматически добавится в заявку; 3) укажите материал, толщину, количество и комментарий; 4) отправьте заявку. Система предварительно оценивает длину реза, время обработки и стоимость лазерной резки, после чего специалист проверяет геометрию и производственные параметры.";
    }
    if (/длина реза|контур|время обработ|стоим/.test(text)) {
      return "Система автоматически обрабатывает контур DXF и использует его для предварительной оценки длины реза, времени обработки и стоимости лазерной резки. Итоговые производственные параметры подтверждает специалист после проверки файла, материала и толщины.";
    }
  }

  return null;
}
