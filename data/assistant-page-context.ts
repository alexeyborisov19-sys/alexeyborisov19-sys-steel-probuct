export type AssistantPageContext = {
  id: "metal-cassette-calculator" | "dxf-online-order" | "general";
  label: string;
  greeting: string;
  knowledge: string;
  suggestions: string[];
};

export const steelProduktBrandKnowledge = [
  "«Сталь Продукт» — бренд, а не наименование юридического лица.",
  "Под брендом представлены инженерные решения и производство изделий из листового металла для строительства, промышленности и инженерной инфраструктуры.",
  "Публично описанная технологическая цепочка включает инженерно-конструкторскую подготовку, лазерный раскрой, гибку, сварку, сборку, подготовку поверхности, порошковую окраску, контроль качества, комплектацию, упаковку и организацию отгрузки.",
  "Монтаж на объекте не выполняется.",
].join(" ");

const generalContext: AssistantPageContext = {
  id: "general",
  label: "Консультация по производству",
  greeting: "Опишите изделие, производственную задачу или спросите о возможностях «Сталь Продукт». Я помогу разобраться и подготовить технически корректную заявку.",
  knowledge: `Посетитель находится на общей странице сайта. ${steelProduktBrandKnowledge} Отвечай по подтверждённым данным о продукции, технологиях, подготовке исходных данных и порядке передачи задачи инженеру. Не придумывай характеристики, цены, сроки, наличие или нормативное соответствие.`,
  suggestions: [
    "Что вы производите?",
    "Возможности производства",
    "Как подготовить чертёж?",
    "Как получить расчёт?",
  ],
};

const contexts: Array<{ match: (pathname: string) => boolean; value: AssistantPageContext }> = [
  {
    match: (pathname) => pathname === "/calculator-metallokassety",
    value: {
      id: "metal-cassette-calculator",
      label: "Калькулятор металлокассет",
      greeting: "Вы открыли калькулятор металлокассет. Могу объяснить расчёт по площади и по стене, открытый и закрытый тип кассет, выбор толщины, базовую цену за м² и как читать предварительный результат.",
      knowledge: [
        steelProduktBrandKnowledge,
        "Текущая страница — публичный калькулятор металлокассет.",
        "Есть два режима: быстрый расчёт по площади фасада и более точная оценка по ширине и высоте стены. В режиме стены можно отдельно указать суммарную площадь окон и дверей.",
        "Пользователь выбирает открытую или закрытую кассету. Для открытого типа учитываются межкассетные швы; для закрытого типа используется отдельная логика замкового стыка.",
        "В публичном расчёте доступны толщины 0,65; 0,7; 1,0 и 1,2 мм.",
        "Для выбранного типа и толщины автоматически подставляется базовая ставка за 1 м². Пользователь может изменить её вручную для конкретной предварительной оценки и вернуть к базовому значению.",
        "Типовой формат, показанный в калькуляторе, — 1170 × 545 мм.",
        "Результат даёт ориентировочную площадь для расчёта, количество кассет, ставку за м² и предварительную стоимость. Массу и производственный срок этот публичный калькулятор не рассчитывает.",
        "Положение проёмов, углы, примыкания, доборные элементы, покрытие, цвет и реальная фасадная раскладка могут изменить итог. Финальная спецификация и цена подтверждаются после инженерной проверки проекта.",
        "Калькулятор не формирует производственный DXF или развёртку; производственная геометрия подтверждается после инженерной проверки заказа.",
        "Не выдавай предварительный результат калькулятора за окончательное коммерческое предложение.",
      ].join(" "),
      suggestions: [
        "По площади или по стене?",
        "Чем отличается открытая кассета от закрытой?",
        "Как читать результат расчёта?",
        "Когда нужен проект инженеру?",
      ],
    },
  },
  {
    match: (pathname) => pathname === "/online-order",
    value: {
      id: "dxf-online-order",
      label: "Онлайн-расчёт по CAD-файлу",
      greeting: "Вы открыли онлайн-расчёт по CAD-файлу. Могу объяснить, что система автоматически читает из DXF и STEP/STP, что нужно указать вручную, как обрабатывается DWG и когда расчёт передаётся инженеру.",
      knowledge: [
        steelProduktBrandKnowledge,
        "Текущая страница — публичный онлайн-расчёт по CAD-файлу.",
        "Для автоматического анализа поддерживаются DXF, STEP и STP. DWG можно добавить в проект, но его геометрию уточняет инженер.",
        "По DXF система определяет габариты, чистую площадь, длину реза и количество врезок по рабочим контурам. Служебные слои вроде размеров, осевых, рамок и штампов не должны попадать в расчёт.",
        "По STEP/STP система анализирует 3D-модель, определяет геометрию, толщину листа и количество гибов, когда модель позволяет сделать это однозначно. Извлечённые параметры нужно проверить перед расчётом.",
        "Материал, количество и производственные операции пользователь задаёт или проверяет в конфигурации позиции. Для DXF количество гибов и другие операции, которых нет в плоском контуре, указываются отдельно.",
        "В одном проекте интерфейс принимает до 10 позиций.",
        "Файлы обрабатываются сервером для построения предпросмотра и расчёта геометрии. Они не отправляются в производственную работу автоматически: передача происходит только вместе с заявкой, которую пользователь отправляет сам.",
        "Предварительная стоимость использует подтверждённые данные расчёта и актуальную цену металла. Если геометрия неоднозначна, данных недостаточно или подтверждённой цены нет, позиция передаётся инженеру вместо приблизительного подтверждения.",
        "Окончательные цена, сроки, характеристики и условия поставки подтверждаются после инженерной проверки коммерческим предложением и договором.",
      ].join(" "),
      suggestions: [
        "Как подготовить DXF?",
        "Что определяется из STEP?",
        "Что нужно указать вручную?",
        "Что происходит с DWG?",
      ],
    },
  },
];

export function normalizeAssistantPathname(value?: string | null) {
  if (!value || typeof value !== "string") return "/";
  const pathname = value.split(/[?#]/, 1)[0].trim();
  if (!pathname.startsWith("/") || pathname.length > 180) return "/";
  return pathname.replace(/\/{2,}/g, "/");
}

export function getAssistantPageContext(value?: string | null): AssistantPageContext {
  const pathname = normalizeAssistantPathname(value);
  return contexts.find(({ match }) => match(pathname))?.value ?? generalContext;
}

export function assistantSuggestionsForPage(
  query: string,
  pathname: string | null | undefined,
  fallback: string[],
) {
  const context = getAssistantPageContext(pathname);
  const text = query.toLowerCase();
  const asksAboutPage = /калькулятор|как пользоваться|поле|формат|результат|dxf|step|stp|dwg|загруз|длина реза|врез|площад|стен|проём|толщин|гиб|кассет|ставк/.test(text);
  return context.id !== "general" && asksAboutPage ? context.suggestions : fallback;
}

export function pageSpecificKnowledgeAnswer(query: string, pathname?: string | null) {
  const context = getAssistantPageContext(pathname);
  const text = query.toLowerCase();

  if (/сталь продукт|о вас|про вас|расскаж.*бренд|расскаж.*компан|что вы производите|чем занимаетесь|кто вы/.test(text)) {
    return `${steelProduktBrandKnowledge} Помощник может подробнее рассказать о продукции, технологических операциях, подготовке чертежей и порядке получения расчёта.`;
  }

  if (context.id === "metal-cassette-calculator") {
    if (/как пользоваться|как работает|калькулятор|что делать|инструкц/.test(text)) {
      return "Калькулятор обновляет предварительный результат по введённым параметрам. 1) Выберите режим «По площади» или «По стене». 2) Выберите открытую или закрытую кассету. 3) Введите площадь фасада либо ширину и высоту стены; в режиме стены можно указать суммарную площадь окон и дверей. 4) Выберите толщину 0,65; 0,7; 1,0 или 1,2 мм. 5) Проверьте базовую цену за м² — при необходимости её можно изменить для текущей оценки. Итог остаётся предварительным до проверки фасадной раскладки инженером.";
    }
    if (/формат|размер кассет/.test(text)) {
      return "В публичном калькуляторе показан типовой формат кассеты 1170 × 545 мм. Сам калькулятор предназначен для оценки бюджета по площади или габаритам стены, а нестандартную геометрию и финальную раскладку нужно подтверждать по проекту.";
    }
    if (/открыт|закрыт|руст|замк|тип кассет/.test(text)) {
      return "Открытая кассета использует видимый крепёж и открытый шов; при расчёте стены учитываются межкассетные швы. Закрытая кассета использует скрытый крепёж и замковый стык, поэтому для неё применяется отдельный рабочий шаг и её нельзя считать по формуле открытого типа.";
    }
    if (/проём|окн|двер/.test(text)) {
      return "В режиме «По стене» можно указать суммарную площадь окон и дверей. Это уточняет предварительную площадь, но расположение проёмов влияет на подрезку и количество целых кассет, поэтому итоговую раскладку всё равно подтверждает инженер.";
    }
    if (/толщин/.test(text)) {
      return "В публичном калькуляторе доступны толщины 0,65; 0,7; 1,0 и 1,2 мм. Для каждой комбинации типа кассеты и толщины подставляется базовая ставка за м², которую можно изменить вручную для конкретной предварительной оценки.";
    }
    if (/масса|вес/.test(text)) {
      return "Текущий публичный калькулятор металлокассет не рассчитывает массу. Он показывает коммерческую предварительную оценку по площади, количеству кассет, ставке за м² и стоимости; массу конкретного изделия нужно считать по подтверждённой геометрии и материалу.";
    }
    if (/срок/.test(text)) {
      return "Текущий публичный калькулятор металлокассет не рассчитывает срок производства. График подтверждается после проверки проекта, состава партии и производственного маршрута.";
    }
    if (/результат|стоим|цен|ставк|бюджет/.test(text)) {
      return "Результат калькулятора — предварительная оценка площади, ориентировочного количества кассет и стоимости по ставке за м². Базовая ставка подставляется автоматически для выбранного типа и толщины, но её можно изменить вручную для конкретной оценки. Это не окончательное коммерческое предложение: раскладку, проёмы, углы, покрытие, цвет, доборные элементы и объём партии проверяет инженер.";
    }
  }

  if (context.id === "dxf-online-order") {
    if (/step|stp/.test(text)) {
      return "STEP/STP поддерживается автоматическим анализом. Система читает 3D-геометрию и, когда модель позволяет определить параметры однозначно, извлекает толщину листа и количество гибов. Эти значения нужно проверить перед расчётом; неоднозначную геометрию и производственную развёртку подтверждает технолог.";
    }
    if (/dwg/.test(text)) {
      return "DWG можно добавить в проект, но автоматический расчёт геометрии для него не подтверждается: такая позиция передаётся на ручную проверку инженеру. Для автоматического анализа используйте DXF, STEP или STP.";
    }
    if (/как пользоваться|как работает|загруз|инструкц|что делать/.test(text)) {
      return "Порядок такой: 1) добавьте CAD-файл — DXF, STEP или STP для автоматического анализа; DWG принимается на ручную проверку. 2) Проверьте считанную геометрию и параметры модели. 3) Укажите или проверьте материал, толщину, количество и нужные операции. 4) Нажмите «Рассчитать проект». 5) Если расчёт получен, передайте проект через заявку инженеру. Окончательные цена и срок подтверждаются после инженерной проверки.";
    }
    if (/dxf|длина реза|контур|врез|площад/.test(text)) {
      return "По DXF система автоматически определяет габариты, чистую площадь, длину реза и количество врезок по рабочим контурам. Служебные слои — размеры, осевые, рамки и штампы — в расчёт не должны попадать. Материал, толщину, количество и операции нужно проверить или указать отдельно.";
    }
    if (/файл|куда|хран|яндекс|диск/.test(text)) {
      return "CAD-файл обрабатывается на сервере для построения предпросмотра и расчёта геометрии. Он не отправляется в производственную работу автоматически: передача происходит только вместе с заявкой, которую пользователь отправляет сам. Текущий интерфейс не следует описывать как загрузку в Яндекс Диск.";
    }
    if (/стоим|цен|прайс|итог|результат/.test(text)) {
      return "Онлайн-расчёт даёт предварительную стоимость по распознанной геометрии, выбранной конфигурации и подтверждённой цене металла. Если данных недостаточно, геометрия неоднозначна или актуальная цена не подтверждена, позиция передаётся инженеру вместо приблизительного подтверждения. Окончательные цена и срок фиксируются после инженерной проверки.";
    }
    if (/несколько|много|позиц|файлов/.test(text)) {
      return "В одном проекте можно добавить до 10 позиций. Каждый CAD-файл становится отдельной позицией, после чего проект рассчитывается как единое целое.";
    }
  }

  return null;
}
